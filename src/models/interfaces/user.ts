export interface IUserGenre {
  id: string;
  label: string;
  name: string;
  color: string;
  order: number;
}

export interface WithdrawalType {
  id: string;
  label: string;
  name: string;
  order: number;
}
